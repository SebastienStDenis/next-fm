// Set when a first-run user leaves the welcome flow early, so the dashboard
// stops redirecting them back to it; the settings nudges take over.
export const WELCOME_SKIPPED_COOKIE = "welcome-skipped";
